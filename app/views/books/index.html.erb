
<ul class="book-list">
  <% @books.each do |book| %>
  
  <li>
    <%= link_to book.title, book_path(book.id) %>
    by <%= book.author%>
  </li>
  <% end %>
</ul>

<%= link_to "Add Book", new_book_path %>